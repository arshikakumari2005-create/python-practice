# update()
# The update() method adds key-value pairs or update the value of an existing key.
student={"name":"Arshika",
         "age":20,
         }
print(student)
student.update({"roll_no":22})
print(student)
student.update({"class":"10th"})
print(student)
